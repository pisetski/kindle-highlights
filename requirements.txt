# Kindle Daily Highlights - Local Development Dependencies
# For GitHub Actions, use requirements-actions.txt instead (much smaller)

resend>=2.0.0

# Required for local import/classification only
# First run downloads ~180MB model, torch is ~2GB
transformers>=4.36.0
torch>=2.0.0
